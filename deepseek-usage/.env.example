# Required: 32-byte url-safe base64 key for encrypting stored API keys.
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MASTER_KEY=

# Optional
HOST=0.0.0.0
PORT=8787
DATA_DIR=./data
REFRESH_INTERVAL_SEC=300
