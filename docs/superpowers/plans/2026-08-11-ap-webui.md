# SoftAP + WebUI Implementation Plan

> **For agentic workers:** Implement task-by-task. Steps use checkbox syntax.

**Goal:** SoftAP when offline, board WebUI for config + env status, DeepSeek DS-only 3min poll with NVS cache.

**Architecture:** Extend WifiManager for AP; new WebUi with Arduino WebServer + PROGMEM HTML; DeepSeekMonitor gated by display view + balance NVS.

**Tech Stack:** Arduino WiFi SoftAP, WebServer, ArduinoJson, Preferences NVS.

---

### Task 1: DeepSeek rules + NVS balance cache
### Task 2: WifiManager SoftAP
### Task 3: WebUi module
### Task 4: Wire main/CLI/docs, build, upload
